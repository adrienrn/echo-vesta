version: '3.3'

services:
  app:
    build: '.'
    command: >
      bash -c 'yarn run start'
    environment:
      NODE_ENV: 'development'
    ports:
      - '80:3000'
      - '35729:35729'
    volumes:
      - '.:/app:rw'
      - '/app/node_modules'
